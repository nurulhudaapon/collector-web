TODO
====

A list of things to be improved, in no particular order:

* Try and manage zig with pnpm, rather than system commands
* Disable zls in `*.zx`, duplicated inlay hints
* Enable `zx fmt` on `lint-staged` when it works better
* Make logout POST-only
